const initialVal = {
    isLoading: false,
    user: null,
    error: ''
}

export const authReducer = (state = initialVal, action) => {

    switch (action.type) {

        default:
            return state;

    }

}
